/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // Optional: Add trailingSlash if your hosting provider requires it for static exports
  // trailingSlash: true,
};

export default nextConfig;
